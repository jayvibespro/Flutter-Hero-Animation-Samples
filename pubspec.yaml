name: hero
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons:
  flutter_staggered_grid_view:

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: 

flutter:

  uses-material-design: true


